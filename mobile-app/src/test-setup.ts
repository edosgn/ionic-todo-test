/**
 * Test setup file for Jest.
 * This file is executed before each test suite and sets up
 * the Angular testing environment with Jest-compatible APIs.
 */
import { setupZoneTestEnv } from 'jest-preset-angular/setup-env/zone';

setupZoneTestEnv();
